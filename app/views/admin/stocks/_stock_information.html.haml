%td{id: "stock-id-#{stock.id}", rowspan: stock.specs.count + 1}
  .media
    .media-left
      = image_tag(stock.cover.url, size: "120x120")
    .media-body
      商品編號：#{stock.id}
      %p.help-block
      商品名稱：
      = link_to stock.name, admin_item_path(stock), target: "_blank"
      %p.help-block
      狀態：#{t(stock.status)}
      %p.help-block
      淘寶：#{stock.ntd_cost}
      %p.help-block
      定價：#{stock.price}
      %p.help-block
      優惠：#{stock.special_price}