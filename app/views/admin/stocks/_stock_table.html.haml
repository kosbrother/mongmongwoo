%table.table.table-bordered
  %thead
    %tr
      %th.large-small 商品資訊
      %th 淘寶網站
      %th 樣式名稱
      %th 樣式狀態
      %th 現貨數量
      %th 運送中數量
      %th 建議庫存量
      %th 操作
  %tbody
    - stocks.each do |stock|
      %tr
        %td{id: "stock-id-#{stock.id}", rowspan: stock.specs.count + 1}
          .media
            .media-left
              = image_tag(stock.cover.url, size: "120x120")
            .media-body
              商品編號：#{stock.id}
              %br
              商品名稱：
              = link_to stock.name, admin_item_path(stock), target: "_blank"
              %br
              狀態：#{t(stock.status)}
              %br
              成本：#{stock.ntd_cost}
              %br
              價格：#{stock.price}
        %td{rowspan: stock.specs.count + 1}
          = link_to "連結", stock.url, target: "_blank"
        - stock.specs.each do |spec|
          %tr
            %td
              = link_to spec.style, "#", rel: "popover", data: { "image-url": "#{spec.style_pic_url}", "placement": "left" }, class: "btn btn-default preview"
            %td
              = t(spec.status)
            %td
              = spec.stock_item_quantity
            %td
              = spec.shipping_item_quantity
            %td
              = spec.requested_quantity
            %td
              = link_to_edit_stock_spec(spec)