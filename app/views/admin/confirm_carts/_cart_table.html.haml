%br/
.apple_pagination
  = will_paginate carts, :container => false
.panel-body
  %table.table.table-striped.table-bordered{:border => "0", :cellpadding => "0", :cellspacing => "0"}
    %thead
      %tr
        %th 編號
        %th{style: "width: 10%"} 日期與狀態
        %th 淘寶商家
        %th{style: "width: 70%"} 訂購商品
        %th 確認收貨
    %tbody
      - carts.each do |cart|
        %tr
          %td
            = cart.id
          %td
            = admin_cart_date(cart)
            = t(cart.status)
          %td
            = cart.taobao_supplier_name
          %td
            %table.table.table-bordered
              %thead
                %tr.active
                  %th{style: "width: 10%"} 商品編號
                  %th{style: "width: 35%"} 商品名稱
                  %th{style: "width: 25%"} 商品樣式
                  %th{style: "width: 30%"} 商品數量
              %tbody
                - cart.admin_cart_items.by_cart.each do |item|
                  %tr
                    %td
                      = item.item.id
                    %td
                      = link_to item.item.name, admin_item_path(item.item), target: "_blank"
                    %td
                      = link_to item.item_spec.style, "#", rel: "popover", data: { "image-url": "#{item.item_spec.style_pic_url}", "placement": "left" }, class: "btn btn-default preview"
                    %td
                      = admin_cart_item_quantity(item)
            %table.table.table-bordered
              %tbody
                %tr.active
                  %th 備註
                %tr
                  %td
                    = admin_cart_note(cart)
          %td
            = link_to_confirm_button(cart)
.apple_pagination
  = will_paginate carts, :container => false
%br/