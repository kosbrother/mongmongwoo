%div{:clas => "row"}
  .content-box-large
    %h2
      用戶名稱：#{@user.user_name}
    .user-infos.well
      %p.lead 用戶資料：
      %dl.dl-horizontal
        %dt 真實姓名
        %dd= @user.real_name
        %dt 性別
        %dd= @user.gender
        %dt 註冊地址
        %dd= @user.address
        %dt 註冊電話
        %dd= @user.phone
        %dt UID
        %dd= @user.uid
        %dt Email
        %dd= @user.email
    .user-orders-list.well
      %p.lead 用戶訂單：
      %table.table.table-bordered
        %thead
          %tr
            %th 訂單編號
            %th 訂單金額
            %th 訂單聯絡電話
            %th 訂單Email
            %th 訂單狀態
            %th 訂購明細
            %th 寄送滿意度問卷
        %tbody
          - @user.orders.each do |order|
            %tr
              %td= order.id
              %td= order.total
              %td= order.ship_phone
              %td= order.ship_email
              %td= order.status
              %td= render(partial: "admin/orders/info_and_items_list", locals: { order: order })
              %td= link_to_send_survey_email(order)